# WAF Internal Backend - Routes ModSecurity WAF traffic to customer containers
# The WAF container forwards requests here after inspection.
# Each customer gets a server block mapping their domain to their container port.

# Default server - returns 502 for unknown hosts
server {
    listen 8081 default_server;
    server_name _;
    return 502;
}
