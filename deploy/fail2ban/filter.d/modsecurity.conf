# ModSecurity WAF audit log filter
# Matches blocked requests in ModSecurity JSON audit log
[Definition]
failregex = .*"client_ip"\s*:\s*"<HOST>".*"action"\s*:\s*"blocked"
            .*"client_ip"\s*:\s*"<HOST>".*"intercepted"
ignoreregex =
